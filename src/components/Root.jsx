import React from "react";

import UserListContainer from "./UserListContainer";

const Root = () => {
    return (
        <div>
            <UserListContainer />
        </div>
    );
};

export default Root;
